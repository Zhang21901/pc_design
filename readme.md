# **安装必要工具**

- **Anaconda**：用于创建虚拟环境并管理依赖。
- **MySQL**：用于导入数据库。
- **Node.js**：用于编译前端。
- **Git**：用于拉取 Superset 项目代码。

# 配置环境

1. 根据数据库的信息修改根目录下的.env文件内容
2. 运行setup.sh文件

```bash
chmod +x setup.sh
./setup.sh
```